{
  "name": "geosupply",
  "version": "1.0.0",
  "description": "GEOSUPPLY: Geopolitical Oracle-Mediated Supply Chain Governance Smart Contracts",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network hardhat",
    "gas": "npx hardhat test --reporter gas"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  }
}
